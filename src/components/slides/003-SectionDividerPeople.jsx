// PLACEHOLDER: session copy for the Global Sync Meetup NYC talk, not final.
import SectionDivider from '../layouts/SectionDivider'

export default function SectionDividerPeople() {
  return (
    <SectionDivider
      variant="dark"
      part="01"
      pillar="blue"
      title="The Two Things That Break First"
      description="People and margin fail before anything else. Everything in this session works backward from that constraint."
    />
  )
}
